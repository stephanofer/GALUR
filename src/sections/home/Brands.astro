---
import SectionContainer from "@/components/SectionContainer.astro";
---

<SectionContainer id="brands" class="mt-4">
  <div class="brands-wrapper">
    <!-- Header con diseño split -->
    <div class="brands-header">
      <div class="header-left">
        <span class="header-tag">Confianza</span>
        <h2 class="brands-title">Trabajamos con las mejores marcas</h2>
      </div>
      <div class="header-right">
        <p class="brands-description">
          Más de <strong>10 marcas reconocidas</strong> respaldan nuestra calidad
        </p>
      </div>
    </div>

    <!-- Carrusel infinito de marcas -->
    <div class="brands-carousel">
      <div class="carousel-track">
        <!-- Primera fila - izquierda a derecha -->
        <div class="carousel-row row-forward">
          <div class="brand-card">
            <div class="card-inner">
              <img src="/favicon.svg" alt="Redragon" class="brand-logo" />
              <span class="brand-name">Redragon</span>
            </div>
          </div>
          <div class="brand-card">
            <div class="card-inner">
              <img src="/favicon.svg" alt="HyperX" class="brand-logo" />
              <span class="brand-name">HyperX</span>
            </div>
          </div>
          <div class="brand-card">
            <div class="card-inner">
              <img src="/favicon.svg" alt="VSG" class="brand-logo" />
              <span class="brand-name">VSG</span>
            </div>
          </div>
          <div class="brand-card">
            <div class="card-inner">
              <img src="/favicon.svg" alt="Logitech" class="brand-logo" />
              <span class="brand-name">Logitech</span>
            </div>
          </div>
          <div class="brand-card">
            <div class="card-inner">
              <img src="/favicon.svg" alt="Razer" class="brand-logo" />
              <span class="brand-name">Razer</span>
            </div>
          </div>
          <!-- Duplicados para loop infinito -->
          <div class="brand-card">
            <div class="card-inner">
              <img src="/favicon.svg" alt="Redragon" class="brand-logo" />
              <span class="brand-name">Redragon</span>
            </div>
          </div>
          <div class="brand-card">
            <div class="card-inner">
              <img src="/favicon.svg" alt="HyperX" class="brand-logo" />
              <span class="brand-name">HyperX</span>
            </div>
          </div>
          <div class="brand-card">
            <div class="card-inner">
              <img src="/favicon.svg" alt="VSG" class="brand-logo" />
              <span class="brand-name">VSG</span>
            </div>
          </div>
        </div>

        <!-- Segunda fila - derecha a izquierda -->
        <div class="carousel-row row-reverse">
          <div class="brand-card">
            <div class="card-inner">
              <img src="/favicon.svg" alt="Fifine" class="brand-logo" />
              <span class="brand-name">Fifine</span>
            </div>
          </div>
          <div class="brand-card">
            <div class="card-inner">
              <img src="/favicon.svg" alt="Royal Kludge" class="brand-logo" />
              <span class="brand-name">Royal Kludge</span>
            </div>
          </div>
          <div class="brand-card">
            <div class="card-inner">
              <img src="/favicon.svg" alt="T-Dagger" class="brand-logo" />
              <span class="brand-name">T-Dagger</span>
            </div>
          </div>
          <div class="brand-card">
            <div class="card-inner">
              <img src="/favicon.svg" alt="Astro" class="brand-logo" />
              <span class="brand-name">Astro</span>
            </div>
          </div>
          <div class="brand-card">
            <div class="card-inner">
              <img src="/favicon.svg" alt="Streamplify" class="brand-logo" />
              <span class="brand-name">Streamplify</span>
            </div>
          </div>
          <!-- Duplicados para loop infinito -->
          <div class="brand-card">
            <div class="card-inner">
              <img src="/favicon.svg" alt="Fifine" class="brand-logo" />
              <span class="brand-name">Fifine</span>
            </div>
          </div>
          <div class="brand-card">
            <div class="card-inner">
              <img src="/favicon.svg" alt="Royal Kludge" class="brand-logo" />
              <span class="brand-name">Royal Kludge</span>
            </div>
          </div>
          <div class="brand-card">
            <div class="card-inner">
              <img src="/favicon.svg" alt="T-Dagger" class="brand-logo" />
              <span class="brand-name">T-Dagger</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</SectionContainer>

<style>
  .brands-wrapper {
    max-width: 1400px;
    margin: 0 auto;
    overflow: hidden;
  }

  /* Header */
  .brands-header {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 3rem;
    align-items: end;
    margin-bottom: 4rem;
    padding: 0 2rem;
  }

  .header-left {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .header-tag {
    display: inline-block;
    width: fit-content;
    font-size: 0.85rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--color-secondary);
    padding: 0.5rem 1rem;
    background: rgba(0, 0, 0, 0.04);
    border-radius: 50px;
  }

  .brands-title {
    font-size: clamp(2rem, 4vw, 3.5rem);
    font-family: var(--font-primary);
    font-weight: 450;
    color: var(--color-secondary);
    margin: 0;
    line-height: 1.2;
  }

  .header-right {
    display: flex;
    align-items: end;
  }

  .brands-description {
    font-size: clamp(1rem, 1.8vw, 1.2rem);
    color: var(--color-secondary);
    margin: 0;
    line-height: 1.6;
  }

  .brands-description strong {
    color: var(--color-secondary);
    font-weight: 500;
  }

  /* Carousel */
  .brands-carousel {
    margin-bottom: 4rem;
    position: relative;
  }

  .brands-carousel::before,
  .brands-carousel::after {
    content: '';
    position: absolute;
    top: 0;
    width: 150px;
    height: 100%;
    z-index: 2;
    pointer-events: none;
  }

  .brands-carousel::before {
    left: 0;
    background: linear-gradient(90deg, white, transparent);
  }

  .brands-carousel::after {
    right: 0;
    background: linear-gradient(270deg, white, transparent);
  }

  .carousel-track {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .carousel-row {
    display: flex;
    gap: 1.5rem;
    animation-timing-function: linear;
    animation-iteration-count: infinite;
  }

  .row-forward {
    animation: scroll-left 30s linear infinite;
  }

  .row-reverse {
    animation: scroll-right 35s linear infinite;
  }

  @keyframes scroll-left {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }

  @keyframes scroll-right {
    0% {
      transform: translateX(-50%);
    }
    100% {
      transform: translateX(0);
    }
  }

  .carousel-row:hover {
    animation-play-state: paused;
  }

  /* Brand Cards */
  .brand-card {
    flex: 0 0 280px;
    background: white;
    border: 1px solid rgba(0, 0, 0, 0.1);
    border-radius: 12px;
    padding: 2rem;
    transition: all 0.3s ease;
  }

  .brand-card:hover {
    border-color: var(--color-secondary);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
    transform: translateY(-4px);
  }

  .card-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .brand-logo {
    width: 120px;
    height: 60px;
    object-fit: contain;
    filter: grayscale(100%);
    opacity: 0.5;
    transition: all 0.3s ease;
  }

  .brand-card:hover .brand-logo {
    filter: grayscale(0%);
    opacity: 1;
    transform: scale(1.1);
  }

  .brand-name {
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--color-secondary);
    text-align: center;
    transition: color 0.3s ease;
  }

  .brand-card:hover .brand-name {
    color: var(--color-secondary);
  }

  /* Stats Section */
  .brands-stats {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 3rem;
    padding: 2.5rem 2rem;
    background: rgba(0, 0, 0, 0.02);
    border-radius: 12px;
    margin: 0 2rem;
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }

  .stat-number {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 700;
    color: var(--color-secondary);
    font-family: var(--font-primary);
    line-height: 1;
  }

  .stat-label {
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--color-secondary);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .stat-divider {
    width: 1px;
    height: 40px;
    background: rgba(0, 0, 0, 0.1);
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .brands-wrapper {
      padding: 3rem 0;
    }

    .brands-header {
      grid-template-columns: 1fr;
      gap: 1.5rem;
      margin-bottom: 3rem;
      padding: 0 1.5rem;
    }

    .header-right {
      align-items: start;
    }

    .brand-card {
      flex: 0 0 240px;
      padding: 1.75rem;
    }

    .brands-stats {
      gap: 2rem;
      padding: 2rem 1.5rem;
      margin: 0 1.5rem;
    }
  }

  @media (max-width: 768px) {
    .brands-wrapper {
      padding: 2.5rem 0;
    }

    .brands-header {
      margin-bottom: 2.5rem;
      padding: 0 1rem;
    }

    .brands-carousel {
      margin-bottom: 3rem;
    }

    .carousel-track {
      gap: 1rem;
    }

    .carousel-row {
      gap: 1rem;
    }

    .brand-card {
      flex: 0 0 200px;
      padding: 1.5rem;
    }

    .brand-logo {
      width: 100px;
      height: 50px;
    }

    .brand-name {
      font-size: 0.85rem;
    }

    .brands-stats {
      flex-wrap: wrap;
      gap: 1.5rem 2rem;
      padding: 1.75rem 1rem;
      margin: 0 1rem;
    }

    .stat-divider {
      display: none;
    }

    .stat-number {
      font-size: 2rem;
    }
  }

  @media (max-width: 480px) {
    .brands-wrapper {
      padding: 2rem 0;
    }

    .brands-header {
      margin-bottom: 2rem;
      padding: 0 0.75rem;
    }

    .header-tag {
      font-size: 0.75rem;
      padding: 0.4rem 0.85rem;
    }

    .brands-title {
      font-size: 1.75rem;
    }

    .brands-description {
      font-size: 0.95rem;
    }

    .brands-carousel::before,
    .brands-carousel::after {
      width: 80px;
    }

    .brand-card {
      flex: 0 0 180px;
      padding: 1.25rem;
    }

    .brand-logo {
      width: 90px;
      height: 45px;
    }

    .brands-stats {
      gap: 1rem 1.5rem;
      padding: 1.5rem 0.75rem;
      margin: 0 0.75rem;
    }

    .stat-number {
      font-size: 1.75rem;
    }

    .stat-label {
      font-size: 0.8rem;
    }
  }
</style>