---
import SectionContainer from "@/components/SectionContainer.astro";
---

<SectionContainer id="electronic-catalogs" class="mt-4">
  <div class="catalogs-container">
    <div class="texts">
      <h2 class="catalogs-title">Catálogos electrónicos de Perú Compras</h2>
      <p class="catalogs-description">
        <strong>Somos proveedores certificados</strong> en los catálogos electrónicos
        de Perú Compras, garantizando calidad y confiabilidad en nuestros productos
        y servicios.
      </p>
    </div>

    <div class="gallery">
      <div class="gallery-item">
        <div class="image-wrapper">
          <span class="badge">EXT-CE-2024-26</span>
          <img
            src="/favicon.svg"
            alt="Habitación infantil con muebles LUFE"
            loading="lazy"
          />
          <div class="catalog-title">
            <p>
              Máquinas, equipo y herramientas para jardinería, silvicultura y
              agricultura
            </p>
          </div>
        </div>
      </div>

      <div class="gallery-item">
        <div class="image-wrapper">
          <span class="badge">EXT-CE-2024-23</span>
          <img
            src="/favicon.svg"
            alt="Habitación con escritorio y estanterías LUFE"
            loading="lazy"
          />
          <div class="catalog-title">
            <p>Mobiliario y equipamiento educativo institucional</p>
          </div>
        </div>
      </div>

      <div class="gallery-item">
        <div class="image-wrapper">
          <span class="badge">EXT-CE-2024-21</span>
          <img
            src="/favicon.svg"
            alt="Dormitorio infantil con escritorio verde"
            loading="lazy"
          />
          <div class="catalog-title">
            <p>Equipos y accesorios de oficina</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</SectionContainer>

<style>
  .texts {
    margin-bottom: 3rem;
  }

  .catalogs-title {
    font-size: var(--fs-xl);
    font-family: var(--font-primary);
    font-weight: 400;
    text-align: center;
    margin: 0 0 1.5rem 0;
    color: var(--color-secondary);
    letter-spacing: -0.02em;
  }

  .catalogs-description {
    text-align: center;
    font-size: var(--fs-base);
    line-height: 1.6;
    color: var(--color-text-light);
    margin: 0 auto 3.5rem auto;
    max-width: 700px;
  }

  .catalogs-description strong {
    font-weight: 600;
    color: var(--color-secondary);
  }

  .gallery {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
    margin-bottom: 3rem;
  }

  @media (min-width: 768px) {
    .gallery {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .gallery {
      grid-template-columns: repeat(3, 1fr);
      gap: 2rem;
    }
  }

  .gallery-item {
    display: flex;
    flex-direction: column;
  }

  .image-wrapper {
    position: relative;
    aspect-ratio: 4 / 5;
    overflow: hidden;
    border-radius: 12px;
    background-color: #f5f5f5;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    transition: box-shadow 0.3s ease;
    cursor: pointer;
  }

  .gallery-item:hover .image-wrapper {
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.12);
  }

  .image-wrapper img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition:
      transform 0.4s ease,
      filter 0.3s ease;
  }

  .gallery-item:hover .image-wrapper img {
    transform: scale(1.03);
    filter: brightness(1.05);
  }

  .badge {
    position: absolute;
    top: 12px;
    left: 12px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    font-size: 0.75rem;
    font-weight: 600;
    padding: 0.375rem 0.75rem;
    border-radius: 6px;
    z-index: 2;
    letter-spacing: 0.025em;
    box-shadow: 0 2px 8px rgba(102, 126, 234, 0.3);
    backdrop-filter: blur(8px);
  }

  .catalog-title {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    background: linear-gradient(
      to top,
      rgba(0, 0, 0, 0.8) 0%,
      rgba(0, 0, 0, 0.6) 70%,
      transparent 100%
    );
    padding: 2.5rem 1rem 1rem 1rem;
    z-index: 1;
  }

  .catalog-title p {
    color: white;
    font-family: var(--font-primary);
    font-size: 0.875rem;
    line-height: 1.4;
    margin: 0;
    font-weight: 450;
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.3);
  }

  @media (min-width: 768px) {
    .catalog-title p {
      font-size: 0.9375rem;
    }
  }
</style>
